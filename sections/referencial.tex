\section{REFERENCIAL TEÓRICO}
\label{sec:review}

% 
%%% 
%%% 
%%% 
%%% 
%


\subsection{Hypertext Markup Language (HTML)}
Linguagem de marcação é um conjunto de \emph{tags}(palavras reservadas da linguagem) e regras que ao ser aplicados em um arquivo texto provem informações sobre o documento, tais informações podem ser por exemplo, de como o documento deve ser editado, formatado, exibido e impresso.


A \emph{World Wide Web} sempre teve como linguagem de marcação o HTML. O HTML foi inicialmente desenvolvido para suprir a necessidade de descrever semanticamente documentos acadêmicos, porem, ao passar dos anos ele sofreu significativas adaptações e melhorias para suportar diversos novos tipos de documentos.

Em HTML as \emph{tags} normalmente devem vir em duplas, onde a primeira indica o inicio do texto que sera modificado e a última o fim, como por exemplo:

 \texttt{<title>} Olá mundo \texttt{</title>}

Ao executar este trecho de código, o navegador ira interpretar que o título da página deve ser "Olá mundo". 

A figura 1, mostra um exemplo de um código em HTML, este é dividido em 2 partes principais, \emph{head}(cabeçalho) e \emph{body}(corpo)
o título do documento juntamente com demais informações sobre o mesmo ficam dentro da \emph{tag} \texttt{<head>}.Dentro da \emph{tag} \texttt{<body>} se encontra o conteúdo do documento. Já as \emph{tags} \texttt{<h1>},\texttt{<h2>},\texttt{<h3>} representam tópicos no documento, onde \texttt{<h1>} é um elemento mais importante que \texttt{<h2>} e que \texttt{<h3>}. Navegadores normalmente renderizam os tópicos mais importantes com fontes maiores do que tópicos menos importantes (cit1).

  


\begin{figure}[!h!t!b]
\centering
\includegraphics[width=7.5cm]{img/imagem1-tcc}
\caption{Exemplo de documento em html}
\end{figure}


A figura 2, apresenta a interpretação do código anterior pelo navegador \emph{Google Chrome}


\begin{figure}[!h!t!b]
\centering
\includegraphics[width=7.5cm]{img/imagem4-tcc}
\caption{Interpretação de código HTML pelo browser Google Chrome.}
\end{figure}

%%
%%%%
%%%%%%
\subsection{Javascript}
Linguagem de \emph{scpript}(roteiro) é uma linguagem de programação usada para manipular customizar e otimizar recursos já 
existentes em um programa. Em tais sistemas, funcionalidades úteis já são disponíveis através de uma interface para o usuário,
a linguagem de script é um mecanismo para expor tal funcionalidade para o controle da aplicação.(2 ecm doc)


Na programação orientada a objetos implementa-se um conjunto de classes que definem os objetos presentes na aplicação, cada classe determina o comportamento(definido no métodos) e estados possíveis(atributos) de seus objetos, assim como o relacionamento com outros objetos.


Javascript é uma linguagem de \emph{script} com suporte a orientação a objetos, quando utilizada como \emph{client-side}(lado do cliente) permite que conteúdo executável seja adicionado a páginas \emph{web},o que possibilita que estas interajam com o usuário, controlem o navegador e criem conteúdo HTML dinamicamente.


A figura 3, apresenta um documento em html que possui um trecho de código em Javascript, note que este trecho se encontra dentro da \emph{tag} \texttt{<script>}, esta informa ao navegador que o trecho a seguir trata-se de um código em Javascript.
Na linha 11 do código apresentado, podemos observar a chamada da função \emph{write} do objeto \emph{document}, esta função tem como objetivo adicionar texto a página que será exibida pelo navegador.

\begin{figure}[!h!t!b]
\centering
\includegraphics[width=7.5cm]{img/imagem5-tcc}
\caption{Código em HTML com trecho em Javascript}
\end{figure}




%
%%
%%%%
%%%%%%
\subsection{PhantomJS}
Phatomjs é um navegador opensource webkit, programável em javascript que possui suporte para diversos padrões web como: manipulação da DOM,  seletores CSS, JSON, Canvas e SVG.
Com o phatomjs se é possivel:

\begin{itemize}
\setlength{\itemsep}{-0.3ex}
\item Realizar testes unitários para aplicações web.
\item Carregar, analisar e renderizar páginas web.
\item Obter screenshots de websites.
\end{itemize}

A seguir um exemplo de código em javascript que ao ser executado pelo phatomjs gera um screenshot do website  http: //phantomjs.org/

\begin{figure}[!h!t!b]
\centering
\includegraphics[width=7.5cm]{img/example}
\caption{FIXME Legenda}
\end{figure}

\begin{figure}[!h!t!b]
\centering
\includegraphics[width=7.5cm]{img/imagem3-tcc}
\caption{FIXME Legenda}
\end{figure}


%
%
%
%%%%%%%%%%
\subsection{Java}
A linguagem de programação java  começou a ser desenvolvida em 1991 com a finalidade de ser utilizada em dispositivos eletrônicos inteligentes, porem com  o crescimento exponencial da World Wide Web em 1993, os engenheiros da Sun Microsystems notaram o potencial da linguagem para aprimorar a funcionalidade de servidores web e a adaptou para estas funções.
A seguir algumas vantagens da linguagem de programação java:

\begin{itemize}
\setlength{\itemsep}{-0.3ex}
\item Possui milhares de bibliotecas já implementadas para diversas funcionalidades, tais bibliotecas poupam significativamente o trabalho do desenvolvedor.
\item A tecnologia java oferece uma plataforma de desenvolvimento orientada a objetos limpa e eficiente.
\item É uma linguagem simples, fácil de aprender e que não exige um conhecimento muito grande do desenvolvedor para se ter produtividade.
\end{itemize}

%%
%%%%
%%%%%%
\subsection{MySQL}
SGBD ou sistema gerenciador de banco de dados, é um conjunto de programas que tem como objetivo armazenar,alterar e apagar informações de uma base de dados. Estes são bastante uteis em aplicações web, por permitirem que as informações sejam armazenadas e utilizadas automaticamente.
O MySQL é um popular SGBD openSource, que se destaca perante as outras opções no mercado por possuir um excelente desempenho.
O MySQL também possui as seguintes vantagens:

\begin{itemize}
\setlength{\itemsep}{-0.3ex}
\item Portabilidade.
\item Utiliza a linguagem SQL.
\item Compatibilidade com diversas linguagens de programação.
\item Estável e com excelente desempenho.
\item Pouco exigente quanto aos recursos de  hardware.
\item Fácil instalação e configuração
\end{itemize}


%%
%%%%
%%%%%%
\subsection{Hibernate}
(Não esta pronto ainda .. estou tento um pouco de dificuldades para elaborar)
Com o advento da World Wide Web, a demanda por aplicações que fazem o uso de  banco de dados relacionais cresceu significamente, é comum que essas aplicações façam acesso constante ao banco de dados para buscar,alterar e editar informações.
Aplicações que fazem uso de uma linguagem orientada a objtos e possuem banco de dados relacional ..
Hibernate é um framework ORM (mapeamento objeto relacional) openSource lider de mercado,


%%
%%%%
%%%%%
\subsection{VRaptor}